# github.conf — GitHub App / PAT references (Stage 7+)
# Prefer file mounts for private keys and webhook secrets.

# --- GitHub App (preferred) ---
# github.app_id =
# github.installation_id =
# github.app_private_key_path = /run/secrets/github-app.pem

# --- PAT fallback (demos / migration) ---
# github.token =

# --- Webhook HMAC secret (Stage 8) ---
# Prefer a file so the secret is not visible in `docker inspect` env listings.
# github.webhook_secret_file = /run/secrets/github-webhook-secret
# Inline override (less preferred):
# github.webhook_secret =

# github.commit_author_name = ApiMorph Bot
# github.commit_author_email = apimorph-bot@users.noreply.github.com
