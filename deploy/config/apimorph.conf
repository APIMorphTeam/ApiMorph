# apimorph.conf — master ApiMorph operator config (FreeRADIUS-style)
# Uncomment settings to enable. Secrets stay in deploy/secrets/ or env — never commit them.
#
# Load order: these .conf files, then environment variables (Docker .env overrides).

# Path notes (Docker Compose):
#   Host:    deploy/config/  ->  Container: /config
#   Secrets: deploy/secrets/ ->  /run/secrets (when mounted)

# include = github.conf
# include = triggers.conf
# include = scan.conf

# Global
# apimorph.config_version = 1
